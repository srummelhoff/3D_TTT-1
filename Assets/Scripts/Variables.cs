namespace Game.Variables
{
    public enum Team { none, X, O };
    public enum Player { One, Two };
    public enum Axis { x, y, z };
    public enum GameMode { FullCubeFromStart, BuildCubeBottomToTop }
}